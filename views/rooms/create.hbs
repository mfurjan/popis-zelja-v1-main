<div class="mt-10">
  <h3 class="text-2xl font-semibold text-purple-600 mb-4">Dodajte želju:</h3>

  <form method="post" action="/rooms/create" class="bg-white p-6 rounded-xl shadow-lg space-y-4 grid grid-cols-1 max-w-md gap-4">
    
    <div>
      <label for="name" class="block text-gray-800 font-medium">Što želite?</label>
      <br>
      <input type="text" name="name" id="name" placeholder="Unesite naziv želje..." minlength="3" maxlength="100" required
        class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
    </div>

    <div class="flex space-x-2">
      <input type="submit" value="Dodaj"
        class="w-full p-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300" />
      <input type="reset" value="Poništi"
        class="w-full p-3 bg-slate-500 text-white rounded-lg hover:bg-slate-700 focus:outline-none focus:ring-2 focus:ring-slate-500 transition duration-300" />
    </div>
  </form>
</div>

{{#if error_validation}}
<p>Dogodila se greška u provjeri podataka!</p>
{{/if}}

{{#if error_database}}
<p>Dogodila se greška prilikom spremanja u bazu podataka!</p>
{{/if}}

{{#if success}}
<p>Želja je uspješno dodana!</p>
{{/if}}